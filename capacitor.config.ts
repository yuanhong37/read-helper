import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.poc.readhelper',
  appName: 'read-helper',
  webDir: 'dist/read-helper/browser'
};

export default config;
